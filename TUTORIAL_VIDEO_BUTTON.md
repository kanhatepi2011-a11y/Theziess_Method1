# Tutorial video button

The bottom navigation now includes **Tutorial**. It opens `/tutorial.mp4` inside an in-app video modal.

Replace the file `tutorial.mp4` in the project root with your final tutorial video. Keep the same file name, or update the `<source src="/tutorial.mp4">` value in `index.html`.
