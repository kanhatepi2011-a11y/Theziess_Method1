# Local no-login build

Telegram authentication and subscription gates are bypassed for local use.
Video processing remains client-side in the browser/Web Worker.

Run:
```bash
npm install
npm run dev
```
