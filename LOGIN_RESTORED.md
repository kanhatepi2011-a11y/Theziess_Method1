# Telegram Login Restored

This build keeps the audio-only MP4 patcher with stco/co64 support and restores the Telegram login gate.

Localhost note: Telegram OAuth/callback must be configured for the URL in your environment. If Telegram rejects localhost, use the configured public HTTPS URL or tunnel.
